{
    "name": "hyperscript-transpiled",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "test": "vitest",
        "coverage": "vitest run --coverage",
        "generate_parser": "cd src/hyperscript-transpiler/; peggy -o parser.cjs grammar.pegjs"
    },
    "devDependencies": {
        "@vitest/coverage-istanbul": "^0.29.8",
        "peggy": "^3.0.2",
        "typescript": "^4.9.3",
        "vite": "^4.2.0",
        "vitest": "^0.29.8"
    }
}
